# 数据库配置
# 本地开发时使用SQLite，线上部署时使用Vercel Postgres (Neon)

# Neon Postgres 配置（线上环境）
# 在Vercel Dashboard中创建Neon数据库后，会自动注入以下环境变量：
# DATABASE_URL="postgres://username:password@host:port/neon_contact_form_prod" (主要连接字符串)
# DATABASE_URL_UNPOOLED="postgres://username:password@host:port/neon_contact_form_prod" (直连)
# POSTGRES_URL="postgres://username:password@host:port/neon_contact_form_prod"
# POSTGRES_PRISMA_URL="postgres://username:password@host:port/neon_contact_form_prod?pgbouncer=true&connect_timeout=15"
# POSTGRES_URL_NON_POOLING="postgres://username:password@host:port/neon_contact_form_prod"
# POSTGRES_USER="username"
# POSTGRES_HOST="host"
# POSTGRES_PASSWORD="password"
# POSTGRES_DATABASE="neon_contact_form_prod"

# 🎯 您的数据库名称：neon_contact_form_prod
# ✅ 符合命名规范：项目功能_环境标识

# 数据库命名建议：
# 1. 使用小写字母和下划线（snake_case）
# 2. 包含项目名称和环境标识
# 3. 示例：my_website_prod, contact_system_production, website_db_prod
# 4. 避免使用特殊字符、空格和大写字母
# 5. 建议格式：{项目名}_{环境}，如：mywebsite_prod

# 本地开发配置
# 本地开发时会自动使用SQLite数据库，无需配置
# 数据库文件位置：./data/contact.db

# 其他配置
NEXT_PUBLIC_APP_URL=http://localhost:3000